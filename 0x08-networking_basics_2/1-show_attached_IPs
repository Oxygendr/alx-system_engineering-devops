#!/usr/bin/env bash

# get all IPv4 addresses assigned to the machine
ips=$(ifconfig | grep "inet " | awk '{print $2}')

# loop through the IPs and check if they are active
for ip in $ips; do
  if ping -c 1 $ip &> /dev/null; then
    echo "Active IP: $ip"
  fi
done

